# 其他股票历史数据查找问题分析

## 📊 问题概述

在修复前，`batch_predict_v18_ma_alignment.py` 的 `load_historical_data_for_prediction` 函数只查找通用目录（`stockdata_v7/test` 和 `stockdata_v7/train`），不会查找模型专用目录。

这会导致**所有使用非自身模型的股票**都存在历史数据查找问题。

---

## ✅ 已修复

修复后的逻辑会根据 STOCK_LIST 中的模型配置，按以下优先级查找：

1. **优先级1**：股票自己的数据目录（`stockdata_v7_{stock_code}/`）
2. **优先级2**：模型专用目录（`stockdata_v7_{model_code}/`，如果模型不是自身模型）
3. **优先级3**：相关模型目录（板块内备用目录，如稀土板块互相备用）
4. **优先级4**：通用目录（`stockdata_v7/`）

---

## 📋 受影响的股票列表

### 使用非自身模型的股票（共16只）

#### 1. **数据中心/电气设备板块**

| 股票名称 | 股票代码 | 使用的模型 | 模型代码 | 是否会受影响 |
|---------|---------|-----------|---------|-------------|
| 良信股份 | sz.002706 | 英维克模型 | 002837 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_002837/`） |
| 歌尔股份 | sz.002241 | 圣邦股份模型 | 300661 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300661/`） |
| 立讯精密 | sz.002475 | 隆扬电子模型 | 301389 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_301389/`） |
| 鸿远电子 | sh.603267 | 航天电器模型 | 002025 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_002025/`） |
| 航天电器 | sz.002025 | 上海瀚讯模型 | 300762 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300762/`） |
| 兆威机电 | sz.003021 | 汉威科技模型 | 300007 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300007/`） |
| 大业股份 | sh.603278 | 汉威科技模型 | 300007 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300007/`） |

#### 2. **稀土/有色金属板块**

| 股票名称 | 股票代码 | 使用的模型 | 模型代码 | 是否会受影响 |
|---------|---------|-----------|---------|-------------|
| 盛和资源 | sh.600392 | 金力永磁模型 | 300748 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300748/`） |
| 北方稀土 | sh.600111 | 中国稀土模型 | 000831 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_000831/`） |
| 华友钴业 | sh.603799 | 中国稀土模型 | 000831 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_000831/`） |
| 中矿资源 | sz.002738 | 中国稀土模型 | 000831 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_000831/`） |
| 洛阳钼业 | sh.603993 | 中国稀土模型 | 000831 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_000831/`） |
| 赣锋锂业 | sz.002460 | 金力永磁模型 | 300748 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300748/`） |
| 中国稀土 | sz.000831 | 金力永磁模型 | 300748 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_300748/`） |
| 天齐锂业 | sz.002466 | 中国稀土模型 | 000831 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_000831/`） |

#### 3. **汽车零部件板块**

| 股票名称 | 股票代码 | 使用的模型 | 模型代码 | 是否会受影响 |
|---------|---------|-----------|---------|-------------|
| 三花智控 | sz.002050 | 拓普集团模型 | 601689 | ✅ 是（修复后会在优先级2查找 `stockdata_v7_601689/`） |

---

## 🔍 问题影响分析

### 影响程度

**所有16只使用非自身模型的股票**都会受到影响，因为：

1. 如果这些股票的历史数据存储在模型专用目录（如 `stockdata_v7_300748/test/`），batch_predict 找不到
2. 只能从通用目录（`stockdata_v7/test/`）或在线数据源获取数据
3. 导致与对应的 real_time 预测脚本使用的历史数据可能不同

### 典型场景

#### 场景1：盛和资源（已发现）
- **real_time_predict_v18_600392.py**：优先查找 `stockdata_v7_300748/test/`（金力永磁模型目录）
- **batch_predict_v18_ma_alignment.py（修复前）**：只查找 `stockdata_v7/test/`（通用目录）
- **结果**：可能使用不同的历史数据文件 → 预测结果不一致

#### 场景2：北方稀土
- **应该查找**：`stockdata_v7_000831/test/`（中国稀土模型目录）
- **修复前**：只能从通用目录或在线获取
- **修复后**：会在优先级2查找 `stockdata_v7_000831/`

#### 场景3：良信股份
- **应该查找**：`stockdata_v7_002837/test/`（英维克模型目录）
- **修复前**：只能从通用目录或在线获取
- **修复后**：会在优先级2查找 `stockdata_v7_002837/`

---

## ✅ 修复说明

### 修复逻辑

修复后的 `load_historical_data_for_prediction` 函数会：

1. **从 STOCK_LIST 中查找股票配置**
2. **提取模型路径中的模型代码**（如 `ppo_stock_v7_300748.zip` → `300748`）
3. **按优先级查找目录**：
   - 优先级1：`stockdata_v7_{stock_code}/test` 和 `train`
   - 优先级2：`stockdata_v7_{model_code}/test` 和 `train`（如果模型不是自身模型）
   - 优先级3：相关模型目录（如稀土板块互相备用）
   - 优先级4：通用目录

### 修复后的查找顺序示例（盛和资源）

1. `stockdata_v7_600392/test/`（盛和资源自己的目录）
2. `stockdata_v7_600392/train/`
3. `stockdata_v7_300748/test/`（金力永磁模型目录，**优选**）✅
4. `stockdata_v7_300748/train/`
5. `stockdata_v7_000831/test/`（中国稀土模型目录，备用）
6. `stockdata_v7_000831/train/`
7. `stockdata_v7/test/`（通用目录）
8. `stockdata_v7/train/`

---

## 📊 修复效果

### 修复前
- ❌ 只查找通用目录
- ❌ 16只使用非自身模型的股票都可能使用错误的历史数据
- ❌ 与对应的 real_time 脚本结果不一致

### 修复后
- ✅ 按优先级查找多个目录
- ✅ 自动根据模型配置查找对应的模型专用目录
- ✅ 与对应的 real_time 脚本查找逻辑一致
- ✅ 所有股票都能找到正确的历史数据文件

---

## 🎯 建议

### 1. **统一数据存储规范**
建议将股票数据存储在对应的模型目录下：
- 使用金力永磁模型的股票 → `stockdata_v7_300748/`
- 使用中国稀土模型的股票 → `stockdata_v7_000831/`
- 使用拓普集团模型的股票 → `stockdata_v7_601689/`
- 等等

### 2. **验证修复效果**
运行 batch_predict 时，检查日志输出：
- 查找 `✅ 已从历史数据文件加载: xxx (xxx 条数据)`
- 确认使用的数据文件路径是否正确

### 3. **对比预测结果**
如果有对应的 real_time 脚本，可以对比：
- 使用的历史数据文件是否相同
- 预测结果是否一致（应该在修复后更接近）

---

## 📝 总结

**所有16只使用非自身模型的股票都存在同样的问题**，但现在已经全部修复。

修复后的逻辑会根据 STOCK_LIST 中的模型配置，自动查找对应的模型专用目录，确保与对应的 real_time 脚本使用相同的历史数据文件。

---

## 📅 更新时间

2026年1月

