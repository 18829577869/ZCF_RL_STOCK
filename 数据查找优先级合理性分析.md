# 数据查找优先级合理性分析

## 📊 对比分析

### real_time_predict_v18_600392.py 的逻辑（参考标准）

```python
可能的路径顺序：
1. stockdata_v7_{stock_code_num}/test/    # 股票自己的目录
2. stockdata_v7_{stock_code_num}/train/
3. stockdata_v7_300748/test/              # 金力永磁模型目录（优选）
4. stockdata_v7_300748/train/
5. stockdata_v7_000831/test/              # 中国稀土模型目录（备用）
6. stockdata_v7_000831/train/
7. stockdata_v7/test/                     # 通用目录
8. stockdata_v7/train/
9. stockdata/test/                        # 旧版兼容
10. stockdata/train/
```

### batch_predict_v18_ma_alignment.py 修复后的逻辑

```
优先级1：股票自己的目录（stockdata_v7_{stock_code}/）
优先级2：模型专用目录（stockdata_v7_{model_code}/），如果模型不是自身模型
优先级3：相关模型目录（稀土板块互相备用：300748 ↔ 000831）
优先级4：通用目录（stockdata_v7/）
```

### batch_predict_v18_ma_alignment.py 修复前的逻辑

修复前可能只查找通用目录，或者逻辑不清晰，没有考虑模型专用目录。

---

## ✅ 修复后的逻辑更合理

### 理由1：与 real_time 脚本保持一致

- **real_time 脚本**：明确优先查找股票自己的目录，然后是模型目录
- **修复后的 batch_predict**：同样优先查找股票自己的目录，然后是模型目录
- **一致性**：两个脚本使用相同的数据查找逻辑，确保预测结果一致

### 理由2：符合数据存储组织方式

```
数据组织方式：
├── stockdata_v7_{stock_code}/          # 每个股票的专用数据目录
│   ├── train/                          # 训练数据
│   └── test/                           # 测试数据
├── stockdata_v7_{model_code}/          # 模型训练时使用的数据目录
│   ├── train/                          # 包含多个相关股票的训练数据
│   └── test/                           # 包含多个相关股票的测试数据
└── stockdata_v7/                       # 通用数据目录
    ├── train/                          # 所有股票的训练数据
    └── test/                           # 所有股票的测试数据
```

**修复后的优先级符合这种组织方式**：
- 首先查找最专用的数据（股票自己的目录）
- 其次查找模型相关的数据（模型训练时使用的数据）
- 最后查找通用数据（兜底方案）

### 理由3：解决实际问题

**问题场景**：盛和资源(600392)使用金力永磁(300748)模型

- **修复前**：只能从通用目录找到数据，可能不是最新的或不是最优的数据
- **修复后**：
  1. 优先查找 `stockdata_v7_600392/`（如果有自己的专用数据）
  2. 然后查找 `stockdata_v7_300748/`（金力永磁模型训练时使用的数据，可能包含盛和资源的数据）
  3. 最后查找 `stockdata_v7/`（通用目录，兜底）

这确保了：
- 如果股票有专用数据，优先使用（最准确）
- 如果使用模型训练时的数据，使用模型相关的数据（与模型匹配）
- 如果都没有，使用通用数据（兼容性）

### 理由4：灵活性和可扩展性

修复后的逻辑根据 `STOCK_LIST` 中的模型配置动态确定查找路径：
- **动态模型映射**：如果将来更换模型，不需要修改代码，只需要更新 `STOCK_LIST`
- **自动模型识别**：从模型路径自动提取模型代码，确定查找目录
- **板块相关备用**：稀土板块等可以互相作为备用目录

### 理由5：符合训练-预测数据一致性原则

**训练时**：模型使用 `stockdata_v7_{model_code}/` 目录下的数据训练

**预测时**：应该优先使用相同目录下的数据，确保：
- 数据格式一致
- 数据预处理方式一致
- 数据特征分布一致

**修复后的逻辑符合这一原则**：
- 优先2会查找模型训练时的数据目录
- 这确保了预测使用的数据与训练数据来源一致

---

## 📋 优先级详细说明

### 优先级1：股票自己的目录
**优点**：
- 数据最准确（针对该股票的专用数据）
- 如果存在，说明有专门的维护
- 可能包含最新的数据

**适用场景**：
- 股票有专门的训练/测试脚本
- 数据单独维护和更新

### 优先级2：模型专用目录
**优点**：
- 与训练时使用的数据一致
- 模型针对这些数据进行了优化
- 可能包含多个相关股票的数据，格式统一

**适用场景**：
- 使用非自身模型（如盛和资源使用金力永磁模型）
- 模型训练时使用了多个相关股票的数据

### 优先级3：相关模型目录
**优点**：
- 同一板块的股票数据可以互相备用
- 例如稀土板块的股票数据相似，可以共用

**适用场景**：
- 板块内股票相关性高
- 某个模型目录的数据可以作为备用

### 优先级4：通用目录
**优点**：
- 兜底方案，确保总能找到数据
- 兼容旧版本的数据组织方式

**适用场景**：
- 前三个优先级都没有找到数据
- 兼容性考虑

---

## ⚠️ 潜在问题

### 问题1：可能找到错误的数据文件

**场景**：股票自己的目录中存在数据，但这些数据可能是旧的或格式不同的

**解决方案**：通过文件匹配（`file.startswith(f"{stock_code}.")`）确保找到正确的文件

### 问题2：多个目录中存在相同文件

**场景**：同一股票的数据在多个目录中都存在，但内容不同

**当前行为**：按优先级顺序，使用第一个找到的文件

**建议**：可以考虑添加数据时间戳比较，选择最新的数据

---

## 🎯 结论

**修复后的逻辑更合理**，理由：

1. ✅ **与 real_time 脚本一致**：确保预测结果一致
2. ✅ **符合数据组织方式**：从专用到通用，逻辑清晰
3. ✅ **解决实际问题**：修复了盛和资源等股票的数据查找问题
4. ✅ **灵活可扩展**：支持动态模型配置
5. ✅ **符合训练-预测一致性原则**：使用与训练时相同的数据源

**建议**：保持修复后的逻辑不变，但可以考虑：
- 添加数据时间戳比较（选择最新数据）
- 添加数据验证（确保数据格式正确）
- 记录使用的数据来源（方便调试和追踪）

---

## 📝 示例：盛和资源(600392)的数据查找顺序

使用金力永磁(300748)模型时的查找顺序：

1. `stockdata_v7_600392/test/sh.600392.盛和资源.csv`（优先级1：自己的目录）
2. `stockdata_v7_600392/train/sh.600392.盛和资源.csv`（优先级1：自己的目录）
3. `stockdata_v7_300748/test/sh.600392.盛和资源.csv`（优先级2：模型目录）
4. `stockdata_v7_300748/train/sh.600392.盛和资源.csv`（优先级2：模型目录）
5. `stockdata_v7_000831/test/sh.600392.盛和资源.csv`（优先级3：相关模型备用）
6. `stockdata_v7_000831/train/sh.600392.盛和资源.csv`（优先级3：相关模型备用）
7. `stockdata_v7/test/sh.600392.盛和资源.csv`（优先级4：通用目录）
8. `stockdata_v7/train/sh.600392.盛和资源.csv`（优先级4：通用目录）

这样确保了：
- 如果有自己的专用数据，优先使用
- 如果使用金力永磁模型，使用模型训练时的数据
- 如果都没有，使用通用数据作为兜底

