# V20牛市牛股训练版使用说明

## 概述

V20版本是针对牛市市场环境优化的预测模型，使用历史牛市牛股数据进行训练，提高模型在牛市中的预测准确性和收益表现。

## 功能特点

1. **牛市牛股训练**：使用历史牛市牛股数据（2007年、2015年、2019-2021年等）进行模型训练
2. **数据优先级优化**：优先使用牛市牛股数据，提高模型在牛市中的表现
3. **兼容性**：完全兼容V19的所有功能

## 使用步骤

### 步骤1：获取牛市牛股数据

运行以下命令获取历史牛市牛股数据：

```bash
python get_bull_market_stocks_data.py
```

这将从baostock获取以下历史牛市牛股的数据：
- **2007年牛市**：云南铜业、中国船舶、中国神华、中国平安、招商银行、贵州茅台、浦发银行
- **2015年牛市**：东方财富、同花顺、中国中车、中国中铁、万科A、上汽集团
- **2019-2021年牛市**：宁德时代、比亚迪、贵州茅台、五粮液、恒瑞医药、平安银行、招商银行、伊利股份、上海机场、海螺水泥

数据将保存到 `stockdata_v20_bull_market/` 目录。

### 步骤2：训练V20模型（可选）

如果需要训练新的V20模型，运行：

```bash
python train_v20_bull_market.py
```

训练参数：
- 总训练步数：3,000,000步
- 并行环境数：8
- 保存频率：每100,000步
- 模型保存路径：`models_v20_bull_market/`

**注意**：如果已经有训练好的模型，可以跳过此步骤。

### 步骤3：使用V20进行预测

#### 方式1：批量预测（使用V20主文件）

直接运行V20主预测文件：

```bash
python batch_predict_v20_ma_alignment.py
```

这将使用V20模型对所有配置的股票进行预测。

#### 方式2：中国卫星单独预测

专门针对中国卫星(600118)的预测：

```bash
python batch_predict_v20_600118_china_satellite.py
```

这将只对中国卫星进行预测，使用V20牛市牛股训练模型。

## 文件说明

### 核心文件

1. **get_bull_market_stocks_data.py**
   - 获取历史牛市牛股数据
   - 数据保存到 `stockdata_v20_bull_market/` 目录

2. **train_v20_bull_market.py**
   - V20模型训练脚本
   - 使用牛市牛股数据进行训练
   - 模型保存到 `models_v20_bull_market/` 目录

3. **batch_predict_v20_ma_alignment.py**
   - V20主预测文件
   - 包含所有V19功能 + V20牛市优化功能
   - 数据查找优先级：牛市牛股数据 > 股票专用数据 > 模型专用数据 > 通用数据

4. **batch_predict_v20_600118_china_satellite.py**
   - 中国卫星专用预测脚本
   - 使用V20模型对中国卫星进行预测

### 数据目录

- `stockdata_v20_bull_market/train/` - 牛市牛股训练数据
- `stockdata_v20_bull_market/test/` - 牛市牛股测试数据
- `models_v20_bull_market/` - V20训练模型
- `logs_v20_bull_market/` - V20训练日志

## 数据查找优先级（V20）

V20版本的数据查找优先级如下（从高到低）：

1. **优先级0**：牛市牛股数据目录（`stockdata_v20_bull_market/`）- V20新增
2. **优先级1**：股票自己的目录（`stockdata_v7_{stock_code}/`）
3. **优先级2**：模型专用目录（`stockdata_v7_{model_code}/`）
4. **优先级3**：相关模型目录（板块内互相备用）
5. **优先级4**：通用目录（`stockdata_v7/`）

## 模型配置

### 中国卫星预测配置

中国卫星(600118)的预测配置：

- **股票代码**：sh.600118
- **股票名称**：中国卫星
- **模型路径**：`models_v20_bull_market/best/best_model.zip`（优先）
  - 备用：`models_v20_bull_market/ppo_stock_v20_bull_market_final.zip`
  - 再备用：`ppo_stock_v7_000547.zip`（航天发展模型）

## 注意事项

1. **数据获取**：首次使用需要运行 `get_bull_market_stocks_data.py` 获取牛市牛股数据
2. **模型训练**：如果使用V20模型，建议先训练模型（可选，也可以使用已有模型）
3. **模型路径**：确保模型文件路径正确，如果V20模型不存在，会自动使用备用模型
4. **数据完整性**：确保牛市牛股数据完整，至少需要100条训练数据

## 常见问题

### Q1: V20模型不存在怎么办？

A: V20会自动使用备用模型。优先级：
1. V20牛市牛股模型
2. V20最终模型
3. 原V7模型（航天发展模型）

### Q2: 如何知道是否使用了牛市牛股数据？

A: 在预测输出中会显示数据来源。如果使用了牛市牛股数据，会显示 `stockdata_v20_bull_market/` 目录。

### Q3: 牛市牛股数据获取失败怎么办？

A: 检查网络连接和baostock服务状态。如果部分股票数据获取失败，不影响其他股票的使用。

## 版本历史

- **V20**：牛市牛股训练版，针对牛市市场环境优化
- **V19**：两融余额监测、数据查找优先级优化
- **V18**：日K线均线信息、RSRS指标、筛选条件检测
- **V17**：明确买卖价格建议、集合预测
- **V16**：趋势/震荡双策略、模型回测统计

## 技术支持

如有问题，请检查：
1. 数据文件是否存在
2. 模型文件路径是否正确
3. 依赖库是否安装完整
4. 网络连接是否正常







