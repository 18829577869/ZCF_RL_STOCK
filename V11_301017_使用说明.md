# V11 漱玉平民301017专用版使用说明

## 概述

本版本专门针对**漱玉平民（301017）**进行优化，采用**V11全功能集成版**，整合了V7、V9、V10的所有功能，实现多模型协同工作、智能融合决策。

## 文件清单

1. **get_stock_data_v11_301017.py** - 数据获取脚本
2. **train_v11_301017.py** - 模型训练脚本
3. **real_time_predict_v11_301017.py** - 实时预测脚本（V11全功能集成版）

## V11版本特点

V11是全功能集成版本，整合了以下功能：

### V7功能（基础框架）
- 技术指标计算（KDJ、OBV、RSI、MACD等）
- 多数据源管理（Tushare/AkShare/baostock）
- LLM指标解释
- 成本模型（佣金、过户费、印花税、滑点）
- PPO强化学习模型

### V9功能（时间序列深度学习）
- LSTM/GRU时间序列处理
- 注意力机制
- 动态参数优化
- 自动学习优化

### V10功能（前沿技术）
- Transformer模型
- 多模态数据处理
- 实时数据可视化（端口8082）
- 全息动态模型

### V11新增：智能融合决策系统
- 多模型协同工作
- 加权融合决策
- 一致性检测
- 置信度评估

## 使用流程

### 第一步：获取数据

```bash
python get_stock_data_v11_301017.py
```

此脚本会：
- 获取漱玉平民301017的历史数据
- 获取相关医药商业股票数据（大参林、益丰药房、老百姓、一心堂、九州通等）
- 获取配置股票数据（恒瑞医药、云南白药、美的集团、招商银行等）
- 自动分割训练集和测试集（以2024-12-31为分界）
- 保存数据到 `stockdata_v7_301017/` 目录

### 第二步：训练模型

```bash
python train_v11_301017.py
```

此脚本会：
- 加载训练数据
- 优先使用漱玉平民301017进行训练（30%概率）
- 训练2,500,000步
- 保存模型到 `ppo_stock_v7_301017.zip`
- 保存检查点到 `models_v7_301017/` 目录
- 自动进行回测评估

**注意**：训练后的模型可用于V11全功能集成版实时预测。

### 第三步：实时预测

```bash
python real_time_predict_v11_301017.py
```

此脚本会：
- 加载训练好的PPO模型（优先使用301017专用模型）
- 实时获取漱玉平民301017的价格数据
- **多模型融合决策**：
  - PPO模型预测（40%权重）
  - LSTM/GRU预测（20%权重）
  - Transformer预测（20%权重）
  - 全息模型信号（20%权重）
- 支持实时可视化（访问 http://127.0.0.1:8082）
- 自动记录交易日志

## 配置说明

### 初始资金
- 默认初始资金：**2万元**（匹配实盘操作）

### 股票列表
- **核心标的**：漱玉平民301017
- **相关股票**：大参林、益丰药房、老百姓、一心堂、九州通
- **配置股票**：恒瑞医药、云南白药、美的集团、招商银行

### 模型参数
- 训练步数：2,500,000步
- 初始资金：20,000元
- 训练时30%概率使用漱玉平民301017
- 评估时优先使用漱玉平民301017

### V11模型权重配置

默认权重（可在脚本中修改）：
```python
MODEL_WEIGHTS = {
    'ppo': 0.4,        # PPO强化学习模型
    'lstm': 0.2,       # LSTM/GRU时间序列
    'transformer': 0.2,  # Transformer模型
    'holographic': 0.2   # 全息动态模型
}
```

## 数据目录结构

```
stockdata_v7_301017/
├── train/          # 训练数据
├── test/           # 测试数据
└── metadata_v7_301017.csv  # 元数据
```

## 模型目录结构

```
models_v7_301017/
├── best/           # 最佳模型
└── ppo_stock_v7_301017_*.zip  # 检查点模型
```

## 日志目录结构

```
logs_v7_301017/
└── eval/           # 评估日志
```

## V11智能融合决策

V11的核心创新是**智能融合决策系统**，将多个模型的预测结果进行融合：

```
融合决策 = PPO动作 × 40% + LSTM预测 × 20% + Transformer预测 × 20% + 全息信号 × 20%
```

### 融合策略

1. **一致性检测**：多个模型给出相同信号时，提高置信度
2. **价格预测融合**：LSTM和Transformer的预测价格取平均
3. **信号整合**：全息模型的综合信号影响最终决策
4. **动态调整**：根据模型表现动态调整权重（未来功能）

## 实时可视化

V11支持实时数据可视化，启动后访问：
- **可视化地址**：`http://127.0.0.1:8082`
- **功能**：价格走势、技术指标、综合仪表板

## 注意事项

1. **数据获取**：首次使用前必须先运行数据获取脚本
2. **模型训练**：训练时间较长，建议使用GPU加速
3. **实时预测**：需要配置数据源（Tushare/AkShare/baostock）
4. **端口冲突**：V11使用8082端口，确保端口未被占用
5. **计算资源**：V11整合多个模型，建议至少8GB内存
6. **首次运行**：首次运行会训练多个模型（LSTM、Transformer等），需要等待

## 与600730版本的对比

| 项目 | 600730版本 | 301017版本 |
|------|-----------|-----------|
| 核心标的 | 中国高科600730 | 漱玉平民301017 |
| 股票代码 | sh.600730 | sz.301017 |
| 行业板块 | 教育 | 医药商业 |
| 相关股票 | 教育、科技相关 | 医药商业相关 |
| 模型文件 | ppo_stock_v7_600730.zip | ppo_stock_v7_301017.zip |
| 数据目录 | stockdata_v7_600730/ | stockdata_v7_301017/ |
| 模型目录 | models_v7_600730/ | models_v7_301017/ |
| 版本 | V7 | **V11（全功能集成）** |

## 常见问题

**Q: 如何查看训练进度？**
A: 使用 `tensorboard --logdir=./logs_v7_301017/` 查看训练曲线

**Q: 如何修改初始资金？**
A: 在 `train_v11_301017.py` 中修改 `INITIAL_BALANCE_V11_301017` 变量

**Q: 如何添加更多相关股票？**
A: 在 `get_stock_data_v11_301017.py` 的 `stocks` 列表中添加股票信息

**Q: V11和V7版本有什么区别？**
A: V11整合了V7、V9、V10的所有功能，支持多模型融合决策，功能更强大

**Q: 如何调整模型权重？**
A: 在 `real_time_predict_v11_301017.py` 中修改 `MODEL_WEIGHTS` 配置

**Q: 可视化无法访问？**
A: 检查8082端口是否被占用，或查看脚本中的可视化配置

## 版本信息

- 版本：V11_301017
- 创建日期：2025-12-06
- 基于：V11全功能集成版
- 适用标的：漱玉平民301017
- 特色：多模型融合决策、全功能集成

## 优势特点

### 1. 功能完整
✅ 整合了V7、V9、V10的所有核心功能
✅ 无需切换版本，一个系统全部搞定
✅ 功能模块化，可按需启用

### 2. 智能融合
✅ 多模型协同工作
✅ 加权融合决策
✅ 一致性检测
✅ 置信度评估

### 3. 灵活配置
✅ 独立启用/禁用各功能模块
✅ 自定义模型权重
✅ 参数可配置

### 4. 完全独立
✅ 不依赖V7/V9/V10的初始化代码
✅ 所有函数在V11中独立实现
✅ 避免版本冲突

---

**V11 - 全功能集成版，让交易决策更智能！** 🎉

